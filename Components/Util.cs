namespace UnlockedData.NoUiSlider
{
    public enum Orientation
    {
        vertical,
        horizontal
    }

    public enum Direction
    {
        ltr,
        rtl
    }

    public enum Behaviour
    {
        drag,
        tap,
        unconstrained,
        none,
        snap,
        hover
    }

    public enum SliderType
    {
        Color,
        DatePicker
    }


    public enum BootstrapColour
    {
        primary,
        success,
        warning,
        info,
        danger
    }
}
